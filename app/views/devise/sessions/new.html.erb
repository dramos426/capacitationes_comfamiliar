<div class="row"> 
  <div class = "span6">
    <fieldset>
      <legend>
        Inicia Sesión
      </legend>
    <div class="well">
      <%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email, :required => false, :autofocus => true, label: "Correo electrónico" %>
          <%= f.input :password, :required => false, label: "Contraseña" %>
          <%= f.input :remember_me, :as => :boolean , label: "Recordarme" %>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "Iniciar Sesión", class: "btn-primary btn-large" %>
        </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
    </fieldset>
  </div>
  <div class="span6" id="well_right">
    <div class="well">
    </div>
  </div>
</div>
